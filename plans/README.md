# Implementation Plans

Generated by the `improve` skill on 2026-06-26 (against commit `e619caf`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Finish the download server/quality picker + restore green typecheck | P1 | M | — | DONE — merged to master (cb00d9c) + OTA shipped to preview |
| 002  | Remove dead video-proxy code and deprecated backend dirs | P2 | S | 001 (soft) | DONE — merged to master (117e949) + OTA shipped to preview |
| 003  | Enable Supabase leaked-password protection (operator action) | P2 | S | — | BLOCKED — needs Supabase dashboard/admin access (no code) |
| 004  | Sanitize episode_queue rows before push fanout | P3 | M | — | DONE — DEPLOYED live as episode-notifier v8 (commit 50d6ee6). See drift note below. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Deploy/merge note (004 drift — important)

The repo's `supabase/functions/episode-notifier/index.ts` was **stale vs the
deployed function** (live v7): it lacked the token-time flood guard
(`predatesToken`/`tokenRegByUser`, `QueueRow.created_at`). Plan 004's sanitize
patch was correctly applied to that stale base, but deploying it as-is would have
**regressed the flood-guard fix**. During the ship, the repo file was reconciled
to the live source + the sanitize patch (commit `50d6ee6`) and deployed as v8.
Lesson for next time: the repo's edge-function files can lag production — always
`get_edge_function` and diff before deploying.

## Dependency notes

- **002 after 001 (soft)**: both edit `app/watch/[episode].tsx`. 001 unbreaks the
  typecheck baseline and 002 also adds the `tsc --noEmit` gate's first green run
  depends on 001 landing. Do 001 first to avoid a conflict; they are otherwise
  independent.
- **003** is a Supabase dashboard/admin action — a code-only executor should mark
  it BLOCKED and hand to an operator.
- **004** edits a Deno edge function; the code change is executor-doable but
  deployment is an operator step.

## Why this order

001 first because the repo's `tsc --noEmit` baseline is currently **red** (4
errors in `components/DownloadPicker.tsx`) and that shipped to `master`; 001 both
finishes the half-built download-picker feature and adds a typecheck gate so it
can't recur. 002 is low-risk deletion of confirmed-dead proxy/server code. 003 is
a one-toggle security win. 004 is a contained hardening of the shared push feed.

## Findings considered and rejected

- **`admin_list_users` / `admin_user_daily` flagged as anon/authenticated-callable
  SECURITY DEFINER**: false alarm — both gate internally on
  `auth.jwt() ->> 'email' = '<admin>'` and `raise exception` otherwise. Verified
  against the live function definitions. Not exploitable.
- **`reports_insert_any WITH CHECK (true)` (anon insert)**: by design — anonymous
  problem reports are intentional (`/report` screen → `reports` table).
- **RLS-enabled-no-policy on `anime_mappings` / `metadata_votes` /
  `metadata_write_limits`**: deny-all to clients; reached only via SECURITY
  DEFINER RPCs. Locked down, not a gap.
- **`record_usage` callable by anon (SECURITY DEFINER)**: low impact (usage stat
  pollution only); not worth a plan.
- **`avatars` public bucket allows listing / `pg_net` in public schema**: minor
  advisor WARNs, low value vs. effort. Skipped.
- **Refactor the giant files** (`app/watch/[episode].tsx` 2745 lines,
  `app/anime/[id].tsx` 1466, `lib/api.ts` 1448, `lib/scraper/direct.ts` 1462):
  real maintainability debt, but HIGH risk to refactor with no component-test
  coverage and no concrete pain reported. Not worth doing speculatively — revisit
  only when a specific change in one of these files keeps being painful, and add
  characterization tests first.

## Not audited (standard-effort scope)

This was a standard-effort audit weighted to hotspots and the security surface. Not
deeply reviewed: the full 2745-line watch-screen playback state machine, the
scraper extractor scripts (`lib/scraper/scripts.ts`, `direct.ts`) for correctness
edge cases, performance profiling of the WebView scrape pipeline, and the i18n
completeness. Flag for a `deep` pass if those areas matter.
