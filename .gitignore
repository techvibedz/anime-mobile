node_modules/
.expo/
dist/
.env
.env.local
.env.*.local
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
ios/
android/

# Sensitive / runtime files
server/cookies-*.json
server/cookies-3rb.json
server/cookies-4up.json
ngrok.log
ngrok-url.txt
*.log

# Test files
server/test-*.js

# Server node_modules
server/node_modules/
server/package-lock.json

# Graphify cache (large)
graphify-out/cache/

# IDE
.vscode/
.idea/

# macOS
.DS_Store

# Firebase FCM service-account private key — SECRET, never commit.
# (google-services.json IS committed: it's client config, needed by EAS Build,
#  and is bundled into every APK anyway.)
fcm-service-account.json

# Supabase CLI scratch
supabase/.temp/
